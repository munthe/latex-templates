\usepackage{luacode}
\directlua{require("../common/luafunctions.lua")}
\begin{luacode}
	-- Reading commandline arguments
    db_name = arg[2] or "sample.csv"
	number = arg[3] or 1
	colorsPath = "../common/colors.csv"
\end{luacode}

\usepackage{ifdraft}

\settypeblocksize{276mm}{200mm}{*} % Højde*bredde på side
\setlrmargins{*}{*}{1}
\setulmargins{*}{*}{0.5}
\setheaderspaces{*}{3mm}{*}
\setheadfoot{\baselineskip}{0pt}
\checkandfixthelayout
\pagestyle{empty}
\makeatletter
\makeoddhead{empty}{\@title, sæt \arabic{KortSet}}{\today}{\thepage}
\makeevenhead{empty}{\thepage}{\today}{\@title, sæt \arabic{KortSet}}
\newcommand{\Titel}{\@title}
\setlength{\parindent}{0mm}
\makeatother
\ifdraft{\usepackage{showframe}}{}

\usepackage{datatool}
\DTLsetseparator{,}
\DTLloaddb{kort}{ \directlua{tex.print(db_name)} }

\usepackage{graphicx}
\graphicspath{{./Billeder/}}
\usepackage{pgffor}
\usepackage{tcolorbox}
\tcbuselibrary{skins,hooks}
\newcounter{KortSet}
\newcounter{KortNummer}[KortSet]

\title{\directlua{tex.print(db_name)}}

\begin{document}

\directlua{tex.print(loadColors(colorsPath,number))}
